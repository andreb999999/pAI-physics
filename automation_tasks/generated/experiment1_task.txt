Conduct a three-part empirical investigation designed to test the specific quantitative predictions from both theory stages. Before designing any measurement protocol, retrieve the experimentally testable predictions listed in theory1's theory_summary.md and theory2's theory_summary.md; these predictions should directly determine what you measure and at what resolution.

**Sub-experiment 1: Nuclear Norm Implicit Regularization Validation.** Design a controlled matrix completion or matrix sensing experiment where the ground truth minimum nuclear norm solution is analytically known. Train using both Muon and AdamW (or gradient descent) from the same random initialization on the same objective. Throughout training, record: (a) the nuclear norm ||W_t||_* at each checkpoint, (b) the full singular value spectrum of W_t at logarithmically spaced steps, (c) training loss and generalization error. Test the specific prediction from theory1: does Muon converge to a solution with strictly lower nuclear norm than the baseline at the same training loss level? Use matrix sizes from 50×100 to 200×400 with ground truth ranks 5, 10, and 20. Report nuclear norm ratios and singular value spectrum plots. Additionally, track the singular value spectra of weight matrices during NanoGPT training for both optimizers to look for the low-rank signature predicted by theory1 in a realistic deep network setting.

**Sub-experiment 2: Critical Batch Size Scaling Law Validation.** Using a NanoGPT-style setup (GPT-2 small scale, or character-level LM for tractability), train with Muon across batch sizes B ∈ {8, 16, 32, 64, 128, 256, 512}. For each batch size, perform a learning rate grid search over at least 8 values to find the optimal η. Record the minimum number of gradient update steps to reach a fixed target validation loss. Plot steps-to-target-loss vs. B and identify the empirical critical batch size B_crit_empirical as the inflection point. Overlay the theoretical prediction for B_crit from theory2 directly on this plot. Also plot the optimal η vs. B relationship and overlay the theoretical scaling rule from theory2. Run the same protocol for AdamW as a comparison baseline. Report quantitative agreement or disagreement between theory and empirical measurements with error bars across at least 2 random seeds per configuration.

**Sub-experiment 3: Compute-Matched Muon vs AdamW Benchmark.** Following the rigorous methodology of Choi et al. (2019) and Schmidt et al. (2021), conduct a fair head-to-head comparison on NanoGPT (GPT-2 small and medium if compute permits). Fix the total compute budget identically for both optimizers. For each optimizer, perform a hyperparameter search (learning rate, weight decay, warmup steps) over at least 20 configurations using the same search strategy (random or grid). Report the best validation perplexity under matched compute with 95% confidence intervals over at least 3 random seeds. Crucially, also evaluate a hybrid configuration — Muon for weight matrices, AdamW for embeddings, LayerNorm parameters, and biases — reflecting actual practitioner usage. Produce an efficiency frontier plot (validation loss vs. compute) and a final perplexity comparison table with statistical significance tests.

For all sub-experiments: fix random seeds, document hardware specifications and software versions, record all hyperparameter ranges in experiment_design.json, provide a complete PyTorch Muon implementation (polar decomposition via torch.linalg.svd), and output verification_results.json and publication-quality figures. Explicitly flag wherever empirical results diverge from theoretical predictions and quantify the magnitude of discrepancy. Ensure all code and configs are reproducible.