Paper synthesis run for Muon implicit regularization research.

Inputs available in this resumed workspace:
- Theory artifacts in math_workspace/ (claim_graph.json, proofs/, checks/, theory_summary.md)
- Experiment artifacts in inputs/experiment_results/ (experiment_results.json, experiment_analysis.md, fairness_protocol.json, experiment_plan.md)

Objectives:
1) Synthesize theory + experiment findings into a coherent narrative.
2) Analyze whether empirical evidence supports or contradicts the conjecture that Muon induces implicit L1 regularization.
3) Produce complete paper artifacts and compile PDF.

Required outputs:
- final_paper.tex
- final_paper.pdf
- analysis_connection.md (explicit theory-experiment bridge with per-prediction assessment)
- artifacts_index.md (list of all supporting files and how they are used in the paper)

Paper structure and requirements:

1. Introduction:
   - Motivation: understanding implicit regularization in optimizers is central to deep learning theory.
   - Contribution summary: theoretical framework + empirical validation for Muon's implicit L1 regularization.

2. Related Work:
   - Implicit regularization in SGD, Adam, and related optimizers.
   - Sign-gradient methods and their properties.
   - L1 regularization, LASSO, and sparse optimization.

3. Theory:
   - Present definitions, assumptions, lemmas, and theorems from the claim graph.
   - Include full proof sketches for main theorems (T1, T2, T3).
   - Clearly delineate: PROVED results (verified claims) vs CONJECTURAL extensions (proposed claims).
   - State the hierarchy: scalar case (exact) -> vector case (proved) -> matrix/nonlinear case (conjectural).

4. Experimental Design:
   - Fairness protocol description.
   - Tasks, models, hyperparameter search methodology.
   - Metrics and statistical testing procedures.

5. Results:
   - Optimizer comparison results (tables with means, std, p-values).
   - Sparsity analysis (weight distributions, L1 norms, sparsity fractions).
   - Scale invariance test outcomes.
   - Regularization strength ablation (Muon(eta) vs AdamW+L1(lambda=eta) comparison).

6. Discussion:
   - Per-prediction assessment table: theory prediction -> experimental finding -> verdict (supported/contradicted/untested).
   - Limitations: surrogate experiments, simplified settings, what would change at scale.
   - Connection to broader implicit regularization literature.

7. Conclusion:
   - Summary of what is proved, what is empirically supported, what remains open.
   - Concrete next steps: full-scale NanoGPT experiments, matrix sign function analysis, deeper architecture study.

Paper quality requirements:
- All mathematical statements must use proper LaTeX notation.
- All experimental claims must cite specific numerical results with uncertainty.
- Figures/tables must be referenced in text.
- Include a limitations section that is honest about surrogate experiment caveats.
- Separate proven facts from conjectures throughout.

Execution constraints:
- No external-network tools.
- No VLM tools.
- No sub-agent delegation (do not call *_agent callables); execute directly in manager run.
- Use available local artifacts only.
- Ensure PDF compilation succeeds before terminating.
