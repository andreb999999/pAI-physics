I conjecture that the Muon optimizer -- which applies the matrix sign function (SVD-based orthogonalization) to gradient matrices before taking a step -- has an implicit regularization effect. Specifically, I believe there exists a regularizer R(W) such that training with Muon on a loss L is equivalent (in some formal sense: same fixed points, same trajectory limit, or same minimizer) to training with vanilla gradient descent on a regularized loss L + lambda * R(W).

The matrix sign function projects the gradient onto the set of orthogonal matrices (or matrices with singular values all equal to 1), which suggests the regularizer may be related to spectral properties of the weight matrices -- possibly spectral norm, nuclear norm, or a function of the singular value distribution.

Step 1: Search ArXiv and the web for the following references and read them carefully:
- The Muon optimizer paper (Bernstein et al., "Old Optimizer, New Norm: An Anthology") and Keller Jordan's Muon implementation/results
- Implicit regularization of gradient descent: Gunasekar et al. (2018) "Implicit Regularization in Matrix Factorization"; Arora et al. (2019) "Implicit Regularization in Deep Matrix Factorization"
- Implicit regularization of SGD: Blanc et al. (2020) "Implicit Regularization for Deep Neural Networks Driven by an Ornstein-Uhlenbeck Like Process"; HaoChen et al. (2021) "Shape Matters"
- Any papers on spectral regularization, matrix sign function properties, and optimizer geometry
- Any existing work on implicit regularization of Muon or steepest descent under spectral/nuclear norm

Step 2: Formalize the conjecture and build the claim graph with:
1. Definitions of Muon update rule formally
2. A characterization of the implicit regularizer R(W)
3. An equivalence theorem: "Muon on L is equivalent to GD on L + lambda*R" (in the appropriate formal sense)
4. Supporting lemmas about the matrix sign function algebraic properties

Step 3: Prove all claims in the graph. Try both continuous-time (gradient flow) and discrete-time (finite learning rate) analyses. Start with the linear network / matrix factorization setting where implicit regularization results are cleanest. If the general nonlinear case is too hard, prove the linear case and state the general case as a conjecture.

Step 4: Verify all proofs (symbolic audit + numerical stress testing).

Key formula to investigate: L_1(theta) optimized by A_1 (Muon) ~ L_2(theta) optimized by A_2 (GD), where L_2 = L_1 + regularization term.

Execution constraints:
- Do not use vlm_document_analysis_tool.
- If any tool fails due OpenAI 429 insufficient_quota, skip that tool and continue with available non-OpenAI tools.
- Prioritize completing formal conjectures, claim graph, proofs, and verification over deep per-paper VLM analysis.
- Avoid external network-dependent tools in this run (no fetch_arxiv_papers, no web_search, no open_deep_search); proceed with internal model knowledge and local workspace only.
