Derive the theoretical scaling laws governing how Muon's critical batch size and optimal learning rate depend on batch size B. The decisive geometric feature of Muon is that its update has fixed Frobenius norm ||polar(G)||_F = √min(m,n) regardless of gradient magnitude. Increasing batch size therefore reduces variance only in the update direction, not its magnitude. This property breaks the assumptions underlying standard linear and square-root learning rate scaling rules for SGD and requires a fundamentally new theoretical treatment.

**Step 1: Frame the problem relative to classical GNS theory.** Review McCandlish et al. (2018)'s Gradient Noise Scale and the standard linear scaling rule η ∝ B (Goyal et al. 2017). Identify precisely which assumptions in these derivations fail for Muon: the standard derivations assume update magnitude scales with gradient magnitude, enabling batch size to reduce both directional noise and magnitude noise. For Muon, only directional noise is reduced. Formalize this distinction using the decomposition of update variance into directional and magnitude components.

**Step 2: Stochastic gradient model and alignment analysis.** Model the mini-batch gradient as G_B = μ + ε_B where μ is the true gradient and ε_B ~ N(0, Σ/B) (matrix-variate Gaussian noise with covariance tensor Σ). The key quantity is the expected cosine similarity between the stochastic update polar(G_B) and the ideal update polar(μ): E[⟨polar(G_B), polar(μ)⟩_F / (||polar(G_B)||_F · ||polar(μ)||_F)] as a function of B, μ, and Σ. Derive this expression analytically or bound it tightly. Use perturbation theory for the polar decomposition: apply Wedin's theorem or the Davis-Kahan theorem (adapted for the polar factor) to non-asymptotically bound the angular deviation ||polar(G_B) − polar(μ)||_F as a function of B and the signal-to-noise ratio ||μ||_op / ||ε_B||_op. State the regime under which the first-order perturbation expansion is valid.

**Step 3: Derive the critical batch size formula.** Define B_crit as the batch size at which the expected per-step loss reduction reaches a specified fraction (e.g., 1/2) of the noise-free ideal. Using the alignment analysis and the second-order expansion of expected loss reduction E[ΔL] ≈ -η·E[⟨∇L, polar(G_B)⟩] + (η²/2)·smoothness_term, derive B_crit as an explicit closed-form expression in terms of the singular value spectrum of μ and the noise covariance Σ. Derive the optimal learning rate η*(B) that maximizes expected per-step progress. Show the two scaling regimes: the linear-scaling regime B < B_crit and the saturation regime B > B_crit.

**Step 4: Comparative analysis.** Produce a comparison table showing B_crit and η*(B) scaling for: (1) standard SGD, (2) normalized gradient descent / signSGD, (3) Adam, and (4) Muon. Derive whether Muon has larger or smaller B_crit than SGD as a function of spectral properties. Connect to the 'ball-surface intersection' model for sign-based methods and explain structural similarities and differences with signSGD arising from Muon's matrix-valued geometry.

**Required outputs:** (1) claim_graph.json with the logical structure of all derivations; (2) theory_summary.md containing the main B_crit theorem with proof or tightest achievable bound, the η*(B) scaling rule with explicit regime transitions, and a section of quantitative experimentally testable predictions (specific formula for B_crit that can be directly compared against empirical measurements on NanoGPT); (3) Python/SciPy/NumPy numerical verification on synthetic quadratic losses with known Σ and μ across a range of signal-to-noise ratios and matrix dimensions, comparing empirical alignment to theoretical predictions; (4) a comparison table of scaling laws across optimizers.