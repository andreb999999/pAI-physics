Establish the theoretical foundations of the Muon optimizer's implicit regularization properties. Muon performs steepest descent under the operator (spectral) norm by replacing the gradient G with polar(G) — the closest orthogonal matrix to G under the Frobenius metric, via the polar decomposition G = UP where polar(G) = U. Your primary goal is to rigorously prove that this update rule induces an implicit bias toward minimum nuclear-norm solutions in tractable settings, while carefully documenting where the result holds and where it fails.

**Step 1: Formalize the operator-norm steepest descent interpretation.** Prove that polar(G) solves min_{U: ||U||_op ≤ 1} <U, G>_F, establishing that Muon is exactly steepest descent under the spectral norm. Then use convex duality to show the dual of the spectral norm is the nuclear norm ||W||_* = Σ σ_i(W). Explain the geometric implication: just as ℓ∞-steepest descent (signSGD) biases toward ℓ1 minimization (Gunasekar et al. 2018), operator-norm steepest descent should bias toward nuclear norm minimization.

**Step 2: Continuous-time analysis.** Analyze the gradient flow dW/dt = −polar(∇L(W)) in at least two settings: (1) underdetermined linear matrix sensing, L(W) = ||A(W) − b||² where multiple solutions exist; (2) depth-2 linear matrix factorization W = W₁W₂ with a matrix completion loss. For each, attempt to prove that the flow converges to the minimum nuclear norm interpolant. State all assumptions explicitly (infinitesimal step size, overparameterization, initialization norms, loss landscape structure) and characterize which are necessary vs. sufficient. If a full convergence theorem is unattainable, prove the strongest available partial result: monotonic nuclear norm decrease along the flow, convergence to a KKT point of the nuclear norm minimization problem, or a local result near the minimum nuclear norm solution.

**Step 3: Discrete-time analysis.** Analyze the practical discrete update W_{t+1} = W_t − η·polar(∇L(W_t)). Does the nuclear norm ||W_t||_* decrease monotonically along this trajectory? Derive the conditions on step size η that guarantee this. Identify failure modes: step sizes that cause oscillation, loss landscapes that prevent convergence to minimum nuclear norm solutions, initialization dependencies. Document negative results and counterexamples with the same rigor as positive results — they are first-class scientific outputs.

**Step 4: Spectral interpretation.** Since polar(G) has all singular values equal to 1, each update applies equal spectral modification in all directions. Contrast with gradient descent, which preferentially modifies large singular value directions. Prove or provide evidence that this isotropic spectral update leads to flatter singular value spectra, consistent with low nuclear norm, and produce a statement connecting update structure to rank control.

**Required outputs:** (1) claim_graph.json documenting all lemmas, theorems, conjectures, failure modes, and their logical dependencies; (2) theory_summary.md with precise formal statements, assumptions catalog, proof sketches or full proofs, and practical implications; (3) Python/NumPy numerical verification on small-scale matrix sensing (e.g., 10×20 matrices, multiple true solution ranks) comparing nuclear norm of solutions found by Muon vs. gradient descent vs. the analytical nuclear norm minimizer; (4) a concise list of experimentally testable predictions for the downstream experiment stage, including specific quantities the experiments should measure and expected outcomes.